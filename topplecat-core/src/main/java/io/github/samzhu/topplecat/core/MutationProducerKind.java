package io.github.samzhu.topplecat.core;

/** Whether verification uses ToppleCat's default PIT producer or a reviewer-configured producer. */
public enum MutationProducerKind {
    DEFAULT,
    CUSTOM
}
