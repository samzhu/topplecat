# ToppleCat 0.0.2 Release Documentation Implementation Plan

> **For the downstream documentation agent:** Execute this plan only after the
> approved verify exit-signal production change and regression tests are
> committed. Preserve every unrelated or pre-existing worktree change. Do not
> push, tag, publish, or create a pull request.

**Goal:** Produce an accurate bilingual 0.0.2 release-documentation set that
describes the integrated core-correctness and trustworthy terminal-signal
changes without claiming that an unavailable remote release already exists.

**Authority:** Java/JUnit acceptance tests and typed JSON/YAML rows remain the
executable source of truth. Release notes explain a version; README and guide
workflow text remains normative. Generated reports and evidence remain
projections.

## Global Constraints

- Read
  `docs/superpowers/specs/2026-07-25-topplecat-0.0.2-release-documentation-design.md`
  in full before editing.
- Do not begin while the current source still lets `toppleCatReport` complete
  successfully with aggregate `FAIL` or `INCOMPLETE`.
- Do not modify production code, tests, evidence schemas, safe-feedback
  schemas, Gradle coordinates, or sample build files in this documentation
  change.
- Do not expose reviewer-only values, identifiers, paths, source names,
  failures, attachments, or escrow contents.
- Do not overwrite the live README or guide diff. Re-read it immediately
  before each edit and integrate only the intended paragraph.
- Do not stage another agent's uncommitted files or hunks without explicit
  handoff.
- Do not claim remote availability based on an earlier observation. Recheck
  Git tags, the remote branch, and Maven Central immediately before writing.
- Record successful verification in release notes only after the final
  integrated tree passes both repository verification commands.

## Intended Commit

`docs: prepare 0.0.2 release documentation`

This commit contains documentation only. It follows, and must not be combined
with, the verify exit-signal production commit.

## File Map

### Create

- `docs/releases/0.0.2.md`
- `docs/releases/0.0.2.zh-TW.md`

### Modify

- `README.md`
- `README.zh-TW.md`
- `docs/guide/getting-started.md`
- `docs/guide/authoring.md`
- `docs/guide/verification-and-evidence.md`
- `docs/guide/troubleshooting.md`
- `samples/README.md`
- `samples/junit-cart-orders/TUTORIAL.md`
- `samples/spring-boot-cart-orders/TUTORIAL.md`

## Task 0: Satisfy the Exit-Signal Precondition

**Step 1: Inspect source and history**

Run:

```bash
git status --short --branch
git log --oneline -10
rg -n "ToppleCat verification verdict is|verdict != EvidenceVerdict.PASS" \
  topplecat-gradle-plugin/src/main/java/io/github/samzhu/topplecat/gradle/ToppleCatReportTask.java
rg -n "failsToppleCatVerifyWhenAggregateVerdictIsIncomplete|TaskOutcome.FAILED" \
  topplecat-gradle-plugin/src/test/java/io/github/samzhu/topplecat/gradle/ToppleCatPluginFunctionalTest.java
```

Required evidence:

- the production task throws only after evidence, reports, feedback, stable
  publication, and run archival complete;
- regression coverage proves both `FAIL` and `INCOMPLETE` fail the Gradle task;
- `toppleCatRehide` still completes;
- all-`PASS` and explicit-`DISABLED` scenarios remain successful;
- the implementation is committed separately.

If any item is absent, stop. Do not write future behavior into public
documentation.

**Step 2: Run the narrow implementation proof**

Run the exact narrow tests named by the verify exit-signal implementation
commit, followed by:

```bash
./gradlew :topplecat-gradle-plugin:test
```

Expected: PASS.

## Task 1: Recheck Release Status

Run:

```bash
git tag --list --sort=version:refname
git ls-remote --heads --tags origin
for artifact in topplecat-core topplecat-junit topplecat-report topplecat-gradle-plugin; do
  url="https://repo1.maven.org/maven2/io/github/samzhu/topplecat/$artifact/0.0.2/$artifact-0.0.2.pom"
  code=$(curl -sS -o /dev/null -w '%{http_code}' "$url")
  printf '%s %s\n' "$artifact" "$code"
done
```

Expected for the present release-candidate plan:

- no `v0.0.2` tag;
- the 0.0.2 commits are not on `origin/main`;
- every 0.0.2 Maven Central POM returns `404`.

If external state differs, update all status language to the verified current
state before continuing. Never publish as part of this plan.

## Task 2: Write Matching English and Traditional-Chinese Release Notes

Create both release-note files with equivalent headings and facts. Use these
six sections in the same order:

1. **Status and compatibility**
2. **Expected-value correctness**
3. **JSON numeric correctness**
4. **Reviewer escrow update**
5. **Public-contract mutation scope**
6. **Trustworthy terminal signal**

### Required content

- Label 0.0.2 as prepared / release candidate and not remotely available.
- Keep the intended coordinates visible, but do not tell readers they can
  resolve them from Maven Central yet.
- State Java 25 and the repository Gradle 9.1.0 wrapper used for verification;
  do not invent a broader minimum-Gradle compatibility promise.
- State that evidence and `agent-feedback.json` schemas did not change.
- Explain that only `c.verify(...)` satisfies an expected obligation; `READ`
  and `UNTOUCHED` fail when enforcement is enabled.
- Explain exact recursive mathematical number equality without epsilon, while
  structural differences and number-versus-text still fail.
- Explain the reviewer-only restore → edit → check → review → update workflow.
  Describe validated staged activation, metadata-only counts, old-state
  preservation, and failure recovery. Say atomic moves are requested where the
  filesystem supports them; do not promise unconditional atomicity.
- Explain that default PIT uses `sourceSets.test` only. Hidden retests do not
  contribute to public mutation strength.
- Explain that the final report/verify task fails on aggregate `FAIL` or
  `INCOMPLETE` only after all artifacts are written; `DISABLED` keeps its
  existing aggregate semantics.
- State that no second DSL, CLI, Spec Kit integration, richer safe feedback,
  per-case mutation scoring, remote custody, or attestation was added.

Do not yet say verification passed. Add the verification section after Task 6.

## Task 3: Correct README Installation Status and Add Release Links

Before editing, run:

```bash
git diff -- README.md README.zh-TW.md
```

### English README

- Keep the `Install 0.0.2` heading and intended coordinates.
- Replace “current Maven Central release” with an explicit release-candidate
  warning that the coordinates are pending remote publication.
- Tell source-checkout contributors to run `./gradlew publishToMavenLocal`
  before exercising the independent samples.
- Do not advise normal consumers to use the unavailable coordinates yet.
- Add `docs/releases/0.0.2.md` to the documentation links.
- Integrate the final exit-signal paragraph after the code precondition is met:
  a green `toppleCatVerify` means `PASS`; failed builds still leave evidence,
  reports, and safe feedback.

### Traditional-Chinese README

Make the same factual changes and link
`docs/releases/0.0.2.zh-TW.md`. Keep technical identifiers and task names
identical to English.

After editing, compare the two installation and result sections side by side;
neither language may promise remote availability or different verdict
semantics.

## Task 4: Correct Getting-Started and Sample Documentation

### `docs/guide/getting-started.md`

- Replace “available from Maven Central” and “released distribution” wording
  with prepared-but-unpublished status.
- Show local source-checkout validation with `publishToMavenLocal`.
- Preserve the future Maven Central repository configuration as the intended
  post-publication setup, clearly labelled unavailable for 0.0.2 today.

### Sample index and tutorials

Update all three currently contradictory passages:

- `samples/README.md`: do not call 0.0.2 released or direct readers to consume
  it remotely.
- `samples/junit-cart-orders/TUTORIAL.md`: describe
  `publishToMavenLocal` as the current source-checkout path; make the remote
  installation link future-facing.
- `samples/spring-boot-cart-orders/TUTORIAL.md`: make the same correction.

Do not change sample coordinates or demo commands. They intentionally consume
the locally published 0.0.2 source snapshot.

## Task 5: Add Version Context to Normative Guides

Before editing, run:

```bash
git diff -- docs/guide/authoring.md \
  docs/guide/verification-and-evidence.md \
  docs/guide/troubleshooting.md
```

### Authoring guide

Add one concise link from expected consumption / numeric equality to the
matching language-independent English release note. Do not duplicate the
entire release note or weaken the guide's normative rules.

### Verification guide

- Link escrow evolution and public-only PIT explanations to the 0.0.2 release
  note.
- Replace the temporary old-behavior exit paragraph at its exact location with
  the implemented terminal semantics.
- Preserve unrelated surrounding edits and the reviewer-data boundary.

### Troubleshooting

Replace only the temporary statement that non-`PASS` exits zero. Explain that
the build now fails after artifacts are complete and that `evidence.json`
contains gate-level diagnosis.

## Task 6: Validate the Integrated Documentation

**Step 1: Search for contradictions**

Run:

```bash
rg -n 'current Maven Central release|available from Maven Central|released `0\\.0\\.2`|install `0\\.0\\.2` from Maven Central' \
  README.md README.zh-TW.md docs samples
rg -n 'exit `0`|exit code `0`|regardless of the verdict|independent of the verdict' \
  README.md README.zh-TW.md docs
rg -n 'atomically activates|activates atomically' \
  README.md README.zh-TW.md docs
```

Expected: no stale claims. Review every match instead of blindly accepting an
empty or non-empty result.

**Step 2: Validate Markdown and links**

Run:

```bash
python3 scripts/verify-docs.py
git diff --check
```

Expected: PASS.

**Step 3: Run the repository contract**

Run exactly:

```bash
./gradlew check
GRADLE_CMD=./gradlew scripts/verify-release.sh
```

Expected: both commands exit 0 on the final integrated tree.

**Step 4: Add honest verification evidence**

Only after Step 3 passes, add a short verification section to both release
notes stating that:

- targeted core-correctness and exit-signal regressions passed;
- `./gradlew check` passed;
- `GRADLE_CMD=./gradlew scripts/verify-release.sh` passed.

Rerun:

```bash
python3 scripts/verify-docs.py
git diff --check
```

## Task 7: Scope Audit, Stage, and Commit

Run:

```bash
git status --short --branch
git diff --stat
git diff -- README.md README.zh-TW.md docs samples
```

Confirm:

- only the two release notes and nine listed documentation files belong to the
  release-documentation commit;
- no production/test/build/sample-build source changed;
- no `.topplecat/`, `build/`, credentials, reviewer source, or generated
  reports are staged;
- the exit-signal code is already in a separate earlier commit;
- English and Traditional Chinese release facts match.

Stage exact files, not broad directories:

```bash
git add README.md README.zh-TW.md \
  docs/releases/0.0.2.md docs/releases/0.0.2.zh-TW.md \
  docs/guide/getting-started.md docs/guide/authoring.md \
  docs/guide/verification-and-evidence.md docs/guide/troubleshooting.md \
  samples/README.md samples/junit-cart-orders/TUTORIAL.md \
  samples/spring-boot-cart-orders/TUTORIAL.md
git diff --cached --check
git diff --cached --stat
git commit -m "docs: prepare 0.0.2 release documentation"
```

Do not stage the design/plan files here if they were already committed
separately. Do not push, tag, publish, or create a release.

## Handoff Evidence

Return:

- the exit-signal implementation commit hash;
- the release-documentation commit hash;
- the exact results of both full verification commands;
- the remote/tag/Maven Central status observed immediately before writing;
- confirmation that no public schema changed;
- confirmation that the work remains local and unpushed.
